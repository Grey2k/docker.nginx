version: '2'
services:
  proxy:
    restart: always
    image: nginx:1.11
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "./config/nginx.conf:/etc/nginx/nginx.conf"
      - "./config/conf.d:/etc/nginx/conf.d"